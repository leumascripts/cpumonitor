# cpumonitor
A Python program that monitors the CPU's behavior, potentially killing processes that are stressing it too much, obviously with a whitelist of important and indispensable programs such as explorer.exe and the script itself.
